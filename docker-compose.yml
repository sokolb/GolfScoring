version: "3"
services:
    frontend:
        build:
            context: ./client-app
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
    # backend:
    #   build:
    #     context: ./backend
    #     dockerfile: Dockerfile
    #   ports:
    #     - "5000:5000"
    #   environment:
    #     DB_URI: sqlite:///app.db
    #   volumes:
    #     - backend-data:/app/data
volumes:
    backend-data:
